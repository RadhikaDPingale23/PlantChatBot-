# Model

The `plant_disease_resnet50.pth` file is not included in this repository because it exceeds GitHub's file size limit.

This project uses a trained ResNet50 model for plant disease detection.
